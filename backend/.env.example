# Datenbank
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dasduell?schema=public"

# WICHTIG: ADMIN_PASSWORD und JWT_SECRET werden bei jedem Serverstart
# automatisch neu generiert und in den Server-Logs ausgegeben!

# JWT Token Gültigkeit (optional)
JWT_EXPIRATION="24h"

# Server
PORT=3000
NODE_ENV=development

# CORS
CORS_ORIGIN="http://localhost:5173"
