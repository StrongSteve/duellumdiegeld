# Das Duell um die Geld - Umgebungsvariablen

# Admin Password (REQUIRED - 64 chars, alphanumeric + special)
# Generate with: openssl rand -base64 48 | tr -dc 'a-zA-Z0-9!@#$%^&*()_+-=' | head -c 64
ADMIN_PASSWORD=your-secure-64-character-password-here

# JWT Secret (ÄNDERN IN PRODUKTION!)
JWT_SECRET=super-secret-jwt-key-change-in-production

# Datenbank URL (für lokale Entwicklung ohne Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dasduell?schema=public
